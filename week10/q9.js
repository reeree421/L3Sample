let integers = [2,4,6,8,10];
        let sum = 0;
        let product = 1;
        for(let index = 0; index < integers.length; index++) 
        {
            sum += numbers[index];
            product *= numbers[index];
        }
        document.write("Sum of provided array is: " + sum + "<br>");
        document.write("Product of provided array is: " + product + "<br>");
